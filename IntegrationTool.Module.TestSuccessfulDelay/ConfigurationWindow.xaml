<UserControl x:Class="IntegrationTool.Module.TestSuccessfulDelay.ConfigurationWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="130"/>
            <ColumnDefinition Width="89*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="10"/>
            <RowDefinition Height="26"/>
            <RowDefinition Height="266*"/>
        </Grid.RowDefinitions>
        <Label Content="Delay in milliseconds:" Grid.Row="1"/>
        <TextBox Grid.Column="1" Height="23" Margin="0,3,0,0" Grid.Row="1" Text="{Binding Path=DelayInMilliseconds, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Top" />

    </Grid>
</UserControl>
